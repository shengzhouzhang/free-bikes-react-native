
import raw from '../../data/bikes.json';
import { parseRawToEntity } from '../../utils/parser';

describe('parser util', () => {

  console.log(raw);

  it('should parse raw data into entity', () => {

  });
});
